{
    "name": "ecom-platform",
    "private": true,
    "scripts": {
        "build": "turbo run build",
        "dev": "turbo run dev",
        "lint": "turbo run lint",
        "test": "turbo run test",
        "format": "prettier --write \"**/*.{ts,tsx,md}\""
    },
    "devDependencies": {
        "clsx": "^2.1.1",
        "prettier": "^3.0.0",
        "turbo": "^1.10.0"
    },
    "workspaces": [
        "apps/*",
        "packages/*"
    ]
}
